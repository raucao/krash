require "spec_helper"
require "krash"

describe Krash do
  
  it "should call #notify on every registed notifier and pass the arguments hash"
  
  it "should initialize a new notifier and add it to the notifiers array"
  
  it "should set config options in a Configuration object"
    
  
end
